// create a function that will update the dom
// each time something is added to an order
import { getOrders, getToppings, getCrusts, getSizes } from "../Data/data.js";
import orderState from "../Data/data.js";

const toppings = getToppings();
const crusts = getCrusts();
const sizes = getSizes();

// export const updateOrder = () => {
//   return `<h3>Current Order</h3>

//           Topping: ${orderState?.toppingId}

//   `;
// };

export const addOrder = () => {};
